pipeline {
    agent none
    stages {
        stage('Back-end') {
            steps {
                sh 'date'
            }
        }
        stage('Front-end') {
            steps {
                sh 'date'
            }
        }
    }
}
